import styled from '@emotion/styled';

const Container = styled.div`
  max-width: 900px;
  min-height: 95vh;
  margin: 0 auto;
  padding: 20px;
  background-color: white;
  box-shadow: 0 0 4px 8px rgba(34, 60, 80, 0.2);
`;

export default Container;
